version: "3"
services:
  server:
    build:
      context: .
      dockerfile: deploy/Dockerfile
    volumes:
      - .:/knowlege.uz
    restart: always
    ports:
      - "9999:3000"
    networks:
      - knowledge.uz

networks:
  knowledge.uz:
