version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6.1

    steps:
      - checkout
      - add_ssh_keys

      - run:
          name: Install dependencies
          command: sudo pip install -r deploy/requirements.txt

      - run:
          name: Deploy
          command: sudo python deploy/deploy.py
